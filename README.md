
  # Create design prototype

  This is a code bundle for Create design prototype. The original project is available at https://www.figma.com/design/cS8GK3LoJuVR5I0jlH2lsN/Create-design-prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  